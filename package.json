{
  "name": "aether-relay",
  "version": "0.2.0",
  "description": "High-Performance Webhook Ingestion & Reliable Dispatch Gateway in Rust",
  "type": "module",
  "scripts": {
    "build": "cargo build",
    "test": "cargo test",
    "lint": "cargo clippy -- -D warnings",
    "fmt": "cargo fmt --check",
    "audit": "cargo audit"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.7.1",
    "@commitlint/config-conventional": "^19.7.1"
  }
}
